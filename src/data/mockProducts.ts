// This file is no longer needed as we're using the backend API
// Keeping it for reference but it won't be used in the application

import type { Product } from "../types";

// Note: This mock data is now replaced by real API data from your backend
// The backend API will provide products with proper Cloudinary image URLs
// based on the category folder structure in Cloudinary

export const mockProducts: Product[] = [
  // This array is now empty as we're fetching real data from the API
  // The backend will provide products with images stored in Cloudinary
  // following the folder structure: ecommerce/{category}/{image-filename}
];